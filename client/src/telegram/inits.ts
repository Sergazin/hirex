const web_app = window.Telegram.WebApp;

/*
{
  const btn = window.Telegram.WebApp.MainButton;
  btn.setText("SALAM");
  btn.onClick(() => {
    alert("ALEIKUM");
  });
web_app.disableVerticalSwipes();
web_app.SettingsButton.show();
web_app.MainButton.show();
web_app.SecondaryButton.show();
web_app.BackButton.show();
}
* **/

/*
web_app.requestContact((success, resp) => {
  web_app.showAlert(JSON.stringify(resp.status));
});
* **/
